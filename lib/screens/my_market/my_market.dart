import 'package:flutter/material.dart';

class MyMarketScreen extends StatelessWidget {
  const MyMarketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("MyMarketScreen"));
  }
}
